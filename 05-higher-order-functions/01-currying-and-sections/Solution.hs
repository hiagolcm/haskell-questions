module Solution
  ( addFive
  , half
  , isBig
  , dropThree
  , isVowel
  ) where

-- New: currying / partial application and sections.
-- See description.md for the spec of each function.

addFive :: Int -> Int
addFive = undefined

half :: Double -> Double
half = undefined

isBig :: Int -> Bool
isBig = undefined

dropThree :: Int -> Int
dropThree = undefined

isVowel :: Char -> Bool
isVowel = undefined
