module Solution
  ( signWord
  , letterGrade
  , maxOf3
  , bmiTell
  ) where

-- New: guards (boolean tests after the function head, ending in otherwise).
-- See description.md for the spec of each function.

signWord :: Int -> String
signWord n = undefined

letterGrade :: Int -> Char
letterGrade score = undefined

maxOf3 :: Int -> Int -> Int -> Int
maxOf3 a b c = undefined

bmiTell :: Double -> String
bmiTell bmi = undefined
