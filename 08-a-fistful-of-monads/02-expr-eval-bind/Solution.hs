module Solution where

-- New: using >>= to flatten what would otherwise be a nested case chain
-- when evaluating an expression tree that might fail.
--
-- Define Expr as described in description.md, then implement eval using
-- >>= (not nested case/if).
