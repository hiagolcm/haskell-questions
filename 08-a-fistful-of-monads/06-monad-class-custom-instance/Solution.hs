module Solution where

-- New: the Monad class itself, and writing your own instance -- which
-- requires Functor and Applicative instances first (class Applicative m
-- => Monad m).
--
-- Define Perhaps as described in description.md (deriving Show and Eq),
-- write instance Functor Perhaps, instance Applicative Perhaps, and
-- instance Monad Perhaps, then implement addPerhaps.
