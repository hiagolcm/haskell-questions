module Solution
  ( swapPair
  , indexedList
  , sumOfProducts
  , maxProduct
  , zipNames
  , firsts
  ) where

-- See description.md for the spec of each function.

swapPair :: (a, b) -> (b, a)
swapPair p = undefined

indexedList :: [a] -> [(Int, a)]
indexedList xs = undefined

sumOfProducts :: [(Int, Int)] -> Int
sumOfProducts ps = undefined

maxProduct :: [(Int, Int)] -> Int
maxProduct ps = undefined

zipNames :: [String] -> [String] -> [(String, String)]
zipNames firstNames lastNames = undefined

firsts :: [(a, b)] -> [a]
firsts ps = undefined
