module Solution where

-- New: writing your own Monoid instance (Semigroup + Monoid, since Monoid
-- requires Semigroup as a superclass).
--
-- Define MySum and MyProduct as described in description.md (both deriving
-- Show and Eq), write their Semigroup and Monoid instances, then implement
-- sumAll and productAll.
