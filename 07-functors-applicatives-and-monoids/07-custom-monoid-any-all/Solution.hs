module Solution where

-- New: boolean Monoids -- one combining with ||, one with &&.
--
-- Define MyAny and MyAll as described in description.md (both deriving
-- Show and Eq), write their Semigroup and Monoid instances, then implement
-- anyTrue and allTrue.
