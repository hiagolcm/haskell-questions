module Solution where

-- No new concept -- the same idea as exercise 06, applied to Bool instead
-- of numbers: writing your own Monoid instances for MyAny (logical or)
-- and MyAll (logical and).
--
-- Define MyAny and MyAll as described in description.md (each with a
-- Monoid instance), then implement anyTrue and allTrue.
